#include "BitcoinExchange.hpp"
#include <cstdlib>

int main(int argc, char** argv) {
	if (argc == 2) {
		try {
			//Implementation
			EXIT_SUCCESS;
		} catch (std::exception& e) {
			std::cerr << e.what() << std::endl;
		}
	}
	EXIT_FAILURE;
}
